# @package _global_

defaults:
  - _self_
  - data: tbdt_state
  - model: gvp_tbdt_module
  - trainer: default
  - logger: csv

paths:
  data_dir: ${hydra:runtime.cwd}/data
  output_dir: ${hydra:runtime.cwd}/outputs

seed: 42
study_name: ""
